if [[ $a -gt $b ]];then
    if [[ $b -gt $c ]]; then
        if [[ $c -gt $d ]]; then
            if [[ $d -gt $e ]];then
                if [[ $e -gt $f ]]; then
                    echo "$a es muy grande"
                fi
            fi
        fi
    fi
fi
